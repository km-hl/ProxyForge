# 在 VPS 上部署时，复制此文件并重命名为 .env。
# 客户端订阅密钥。旧版本升级时会原样迁移，现有订阅地址不会变化。
# 留空会自动生成，并保存到 data/config.json。
SECRET_TOKEN=""
# Web 控制台管理密钥，必须与订阅密钥不同，且至少 16 个字符。
# 留空会自动生成到 data/admin_token.txt；登录后请在 WebUI 中更换。
ADMIN_TOKEN=""
# 自定义访问端口，默认为 8000
WEB_PORT="8000"
