services:
  proxyforge:
    build: .
    container_name: proxyforge
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8000}:8000"
    # 所有运行时数据（含 data/template.yaml）统一持久化
    volumes:
      - ./data:/app/data
    # 加载环境变量文件
    env_file:
      - .env
